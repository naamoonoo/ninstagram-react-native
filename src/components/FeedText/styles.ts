import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: {
		paddingHorizontal: 5,
		flexDirection: "row",
		flexWrap: "wrap",
		flexShrink: 1
	}
});

export default styles;
